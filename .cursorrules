# Game of War - Project Context Rules

## Project Overview
This is a Java-based card game implementation of "War" - a simple card game where players battle with cards. The game supports configurable numbers of players, suits, and ranks.

## Technology Stack

### Core Technologies
- **Language**: Java 21
- **Build Tool**: Gradle (build.gradle, settings.gradle)
- **Package Manager**: Gradle dependencies (Maven Central)
- **IDE**: IntelliJ IDEA (gameOfWar.iml, .idea/ directory)
- **CI/CD**: Travis CI (.travis.yml)
- **Gradle Wrapper**: gradlew.bat (Windows), gradlew (Unix)

### Dependencies
- **Testing**: 
  - JUnit 4.13.2 (test scope)
  - Mockito 5.11.0 (test scope)
- **Utilities**:
  - Apache Commons Lang3 3.1
  - Commons Collections 3.2.1

### Java Version
- Java 21 (source and target compatibility)
- Source encoding: UTF-8

## Project Structure

### Package Organization
```
com.groovin101.gow
├── exception/          # Custom exception classes
├── model/              # Domain models (Card, Player, Deck, Dealer, etc.)
├── rules/              # Game rule implementations (RuleChain pattern)
└── War.java           # Main game class and entry point
```

### Directory Layout
```
game-of-war/
├── src/
│   ├── main/java/     # Production code
│   └── test/java/     # Test code
├── build/             # Build output (compiled classes, reports)
├── gradle/
│   └── wrapper/       # Gradle wrapper files
├── build.gradle       # Gradle build configuration
├── settings.gradle    # Gradle project settings
├── gradle.properties  # Gradle properties
├── gradlew.bat        # Gradle wrapper (Windows)
├── gradlew            # Gradle wrapper (Unix)
├── gameOfWar.iml     # IntelliJ module file
└── .travis.yml       # CI/CD configuration
```

## Architecture Patterns

### Domain Models
- **Card**: Represents a playing card with Rank and Suit
- **Player**: Manages player state, deck, and card playing logic
- **Deck**: Card collection with creation, shuffling, and dealing capabilities
- **Dealer**: Handles card distribution to players
- **War**: Main game orchestrator

### Design Patterns
- **Rule Chain Pattern**: Used in `rules/` package (RuleChain interface exists but appears to be in early stages)
- **Exception Hierarchy**: Custom exceptions extend base exception types
- **Factory Pattern**: InputArguments creates game configuration from command-line args

## Code Conventions

### Naming
- Package: `com.groovin101.gow`
- Classes: PascalCase (e.g., `InputArguments`, `WarInitializationException`)
- Methods: camelCase (e.g., `playARound()`, `buildPlayerList()`)
- Variables: camelCase (e.g., `numberOfRoundsPlayed`, `winnerOfTheGame`)

### Testing
- Test classes: `*Test.java` suffix
- Test base class: `BaseTest` in `test.utils` package
- Test framework: JUnit 4 with Mockito for mocking
- Test naming: `test_*` prefix convention (e.g., `testBuildPlayersList_namesAreUnique`)
- Test structure: Uses `@Before` for setup, extends `BaseTest` for shared fixtures

### Exception Handling
- Custom exceptions in `exception/` package:
  - `WarInitializationException` (base)
  - `IncorrectNumberOfArgumentsException`
  - `InvalidNumberOfPlayersException`
  - `InvalidNumberOfRanksException`
  - `InvalidNumberOfSuitsException`
  - `InvalidUsernameException`
  - `NoCardsToPlayException`

## Application Entry Point

### Main Method
- Location: `War.main(String[] args)`
- Command-line arguments:
  - Format: `numberOfPlayers numberOfSuits numberOfRanks [-e]`
  - `-e` flag: Enable exception reporting
  - Defaults: 2 players, 4 suits, 13 ranks (standard deck)

### Game Flow
1. Parse command-line arguments via `InputArguments`
2. Initialize game with `War` constructor
3. Create and shuffle deck
4. Deal cards to players
5. Play rounds until game over:
   - Battle phase (single card)
   - War phase (if tie)
   - Distribute winnings
6. Announce winner

## Key Features

### Game Mechanics
- Configurable deck size (suits × ranks)
- Multiple players support
- War resolution when cards tie
- Automatic player elimination when out of cards
- Draw detection

### Current TODOs (from code comments)
- Add play method that takes list of usernames
- Test for draw scenario with no winner of war
- Instantiate deck properly using args
- Consider renaming `throwdownCards` to `throwdownPile`
- RuleChain implementation appears incomplete

## Build & Run

### Gradle Commands
- Build: `.\gradlew.bat build` (Windows) or `./gradlew build` (Unix)
- Test: `.\gradlew.bat test` (Windows) or `./gradlew test` (Unix)
- Clean: `.\gradlew.bat clean` (Windows) or `./gradlew clean` (Unix)
- Run: `java -cp build/classes/java/main com.groovin101.gow.War [args]`
- Create fat JAR: `.\gradlew.bat fatJar` (creates gameOfWar-all.jar with dependencies)

### Travis CI
- Language: Java
- Build command: `./gradlew build`

## Development Guidelines

### When Adding Features
1. Follow existing package structure
2. Add appropriate custom exceptions
3. Write tests extending `BaseTest` when applicable
4. Use JUnit 4 and Mockito for testing
5. Maintain Java naming conventions

### When Modifying Stack/Tooling
- Update this `.cursorrules` file to reflect changes
- Update `build.gradle` for dependency changes
- Update `settings.gradle` if project structure changes
- Update `.travis.yml` for CI/CD changes
- Update `gameOfWar.iml` if IntelliJ configuration changes

## Notes
- The project uses Java 21 with Gradle 9.2.1
- Testing dependencies: JUnit 4.13.2, Mockito 5.11.0
- Some code has TODOs indicating future improvements
- RuleChain pattern is defined but not fully implemented
- The project structure suggests a clean separation of concerns
- Gradle wrapper is included for consistent builds across environments

---

**Last Updated**: This file should be updated whenever:
- Dependencies change (build.gradle)
- Project structure changes
- New patterns or conventions are introduced
- Stack or tooling evolves
- Architecture significantly changes
- Gradle version or Java version changes

